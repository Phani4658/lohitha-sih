/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i0.wp.com", "encrypted-tbn0.gstatic.com","plus.unsplash.com", 
      "images.unsplash.com"
    ],
  },
};

export default nextConfig;
